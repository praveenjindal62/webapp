sudo: true

service:
  docker

before-install:
  - docker pull praveenjindal62/pyapp:1.1
  - docker pull praveenjindal62/nginx:1.0
  - docker pull mongo:latest
  - docker-compose up -d

scripts:
  - cd test && sh app-action.sh && cd -
  - cd test && sh remove-task.sh && cd -
